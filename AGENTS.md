pi agent 源码地址：D:\code\open-source\pi

## Agent skills

### Issue tracker

本仓库使用 `.scratch/` 下的本地 Markdown 文件跟踪 Issue 与规格。参见 `docs/agents/issue-tracker.md`。

### Triage labels

本仓库使用默认的五类 triage 标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human` 和 `wontfix`。参见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用单上下文领域文档布局：根目录使用 `CONTEXT.md`，架构决策记录存放于 `docs/adr/`。参见 `docs/agents/domain.md`。
