# Pi 子代理协作

本上下文描述 Pi 父会话与其临时子代理之间的协作关系，统一规格和后续实现所使用的领域语言。

## 领域语言

**父会话**：
对某个子代理而言，创建并直接拥有它的 Pi 会话；根 Pi 会话和未达到深度上限的子代理都可以成为父会话，且每个父会话只直接控制自己的直接子代理。
_避免使用_：共享会话、全局会话

**子代理**：
归属于唯一父会话、拥有独立上下文和身份的临时协作实体；它可在父会话内接受多次交互并创建下一层子代理，但不会跨根 Pi 会话结束或恢复而持久化，且只能与直接父会话和直接子代理通信。
_避免使用_：持久代理、共享代理、对等代理

**代理标识**：
在一棵代理树内唯一标识某个子代理的 `agent_id` 值；该值是 UUID，控制器创建新节点时使用随机 UUID v4，并以 RFC 9562 canonical 小写文本格式（`xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`）传输，不带 `agent_` 前缀。它在节点生命周期内保持稳定，节点终止后也不会在当前根会话期间复用。控制操作通过代理标识寻址，显示名称不承担身份含义，代理标识本身不表达父子关系、深度或创建顺序；格式错误的调用参数属于 `invalid_argument`，格式正确但未注册的值属于 `agent_not_found`。
_避免使用_：以代理名称寻址、以层级路径寻址

**控制调用结果**：
父会话发起的一次控制工具调用是否成功完成；它由调用结果的 `ok` 表达，与目标子代理当前是否健康相互独立。

**节点故障**：
子代理当前生命周期中的控制面故障，例如 RPC 断开或启动超时；它可以作为状态查询成功结果中的 `data.error` 出现，不等同于控制调用失败。

**代理树**：
由根 Pi 会话及其递归创建的子代理组成的临时层级；代理树只允许有直接父子关系的节点相互通信，终止任一子代理时会同时终止它拥有的整个子树。
_避免使用_：代理网络、对等代理组

**代理树视图**：
向根 Pi 会话展示整棵代理树拓扑和运行状态的只读信息；查看该视图不赋予根会话越级通信或直接控制后代的权限。
_避免使用_：全局控制台、跨层控制面板

**父子消息**：
父会话向直接子代理发送的唯一任务通信；子代理空闲时该消息启动处理，子代理执行中时该消息引导当前处理，而不会排队成为独立的后续任务。
_避免使用_：对等消息、广播消息、后续任务队列

每条父子消息由控制器分配一个在根会话生命周期内唯一且不复用的消息标识；调用者不能自定义该标识，它只用于关联控制面事件，不表达消息内容或处理结果。

**子代理回复**：
子代理只向直接父会话发送的上行协作内容总称，分为工作中回复和最终答复；未经主动选择的思考过程、工具前说明、工具参数与工具结果不属于子代理回复，也不允许越级发往其他祖先。
_避免使用_：普通 assistant 自动转发、父代理调用、越级回传

**子代理回复展示**：
父会话界面对一条子代理回复的可读投影；发送者名称用于辅助识别，代理标识继续承担身份含义，展示内容不改变模型可见回复信封。
_避免使用_：名称寻址、回复协议改写

**工作中回复**：
子代理在当前处理继续进行时主动发出的进度、问题或阶段性发现；父会话可据此结束一次等待，但该回复不使子代理 settled，也不声明任务已经完成。
_避免使用_：最终结果、隐式过程输出、回复即暂停

**最终答复**：
子代理一次处理在 settled 边界自动提交且至多一次的最终上行结果；它包含本轮最近的可用业务内容，或在未产生内容、失败或中断时给出简短说明。
_避免使用_：工作中汇报、重复 final、message_end 即完成

**协作式中断**：
父会话请求直接子代理停止当前处理但保留节点及其上下文；其完成依赖当前处理响应取消，不承诺清除已接受的父子消息、立即空闲或永久结束节点。
_避免使用_：终止、暂停、清空队列

**终止**：
永久结束一个子代理及其整个子树并回收相关运行资源；终止后的节点不能继续接收父子消息，也不能恢复为可工作状态。
_避免使用_：中断、暂停、隐藏

**终止屏障**：
终止控制器在一个线性化点建立的不可逆关闭意图；它固定目标及当时已登记的后代，拒绝屏障后的普通消息和创建，并阻止迟到事件把节点恢复为可工作状态。屏障不等同于资源已经回收。
_避免使用_：软关闭标记、暂停状态、单节点退出

**清理期限**：
终止流程为优雅关闭、强制回收和最终资源确认设置的内部有界时间窗口；它独立于 `wait_agent.timeout_ms`，不由子代理或普通等待调用扩大。期限结束仍无法确认资源时保留 `terminating`，不假定已经回收。
_避免使用_：等待超时、空闲超时、模型请求超时

**强制进程树回收**：
清理期限到达后使用操作系统进程组能力一次性回收节点及其后代的阶段；Windows 首选 Job Object，Unix 类系统首选 process group/session。信号发送成功不等同于资源回收确认，平台能力不足时不能伪造 `terminated`。
_避免使用_：直接子进程 kill、PID 猜测、单进程终止

**防孤儿清理**：
中间父代理异常退出后，直接父控制器为其已登记的全部后代建立终止屏障并执行级联回收的流程；故障父节点保留 `failed` 和名额，直到直接父会话显式终止并确认整棵子树资源回收。
_避免使用_：自动重启、孤儿代理、子树静默删除

**部分级联失败**：
同一终止子树中部分节点已确认回收、部分节点仍无法确认的状态；已回收节点先进入 `terminated` 并释放名额，未确认节点保持带 `termination_incomplete` 的 `terminating`，父节点要等全部后代完成。
_避免使用_：整批原子成功、失败即重启、忽略未回收节点

**生命周期状态**：
子代理对外可观察的七种阶段：`starting`（正在启动并确认 RPC 可通信）、`idle`（无活动处理）、`working`（正在处理）、`interrupting`（协作式中断处理中）、`failed`（故障不可正常通信）、`terminating`（终止意图已不可逆生效但资源尚未全部确认回收）和 `terminated`（终止及资源回收已确认）。待处理消息不是独立状态；进程意外退出归入 `failed`，终止流程中的预期退出归入 `terminated`。
_避免使用_：把队列当状态、把底层事件名当生命周期

**空闲保留**：
子代理进入 `idle` 后不会因空闲时间自动终止或回收；它持续保留到直接父会话显式终止，或根会话结束时统一清理。等待一次处理的超时只结束等待，不改变节点生命周期。
_避免使用_：空闲回收、等待超时即终止

**终止记录**：
子代理完成终止后，在当前根会话期间保留的身份与最终状态；它用于诊断和幂等控制，不代表仍有运行资源，不占直接子代理名额，也不能恢复或接收父子消息。
_避免使用_：活动代理、持久代理、可恢复节点

**深度上限**：
根 Pi 会话为整棵代理树设定且后代不能提高的最大子代理层级；根 Pi 会话的深度为零，每经过一层父子关系深度加一。根启动参数、已获信任项目配置、用户配置和内置默认值按顺序确定一次有效值，默认值为 `2`，代码硬上限为 `8`；被选中的配置层不可读、JSON 无法解析或值不在 `1..8` 时直接采用默认值而不回退低优先级层，显式根参数非法则拒绝启动。达到上限的子代理不再承担创建、通信或控制下一层子代理的职责。
_避免使用_：无限递归、固定层级

**配额配置诊断**：
根会话解析深度或数量配额配置时产生的用户界面警告。它报告不可读配置、JSON 解析失败、非法已知字段或未知字段被忽略，以及因故采用的默认值；它是 UI-only 信号，不创建会话消息或模型上下文。显式根启动参数非法时不产生运行中的诊断，而是拒绝启动。
_避免使用_：配置模型消息、静默配置回退

**直接子代理上限**：
每个父会话最多允许拥有的直接子代理数量；各父会话独立计数，不按整棵代理树中相同深度的节点总数汇总。根启动参数、已获信任项目配置、用户配置和内置默认值按顺序确定一次有效值，默认值为 `4`，代码硬上限为 `16`。
_避免使用_：层级总量、全树同层上限

**全树代理上限**：
一棵代理树中除根会话外、尚未进入 `terminated` 的全部节点共享的数量上限；它独立于每个父会话的直接子代理上限。根启动参数、已获信任项目配置、用户配置和内置默认值按顺序确定一次有效值，默认值为 `16`，代码硬上限为 `64`；创建预留全树名额，节点及其子树完成资源回收后释放。
_避免使用_：直接子代理上限、同时运行数

**配额耗尽行为**：
达到深度上限时立即拒绝且不可重试；直接子代理或全树名额耗尽时立即拒绝，待调用方显式终止节点并完成资源回收后再重试。配额耗尽不自动等待、排队或回收。
_避免使用_：配额自动调度、隐式资源回收

**并行处理**：
不同子代理可以同时处理任务；扩展不设独立的同时运行数上限。单个子代理的消息、中断和终止仍按其控制通道串行协调，向已在处理的节点发送 steering 不产生额外运行配额。
_避免使用_：全树并发配额、跨节点命令串行

**资源控制范围**：
本版本只限制代理树深度、每个父会话的直接子代理数和整棵树的未终止节点数；不按 token/费用限制模型成本，不限制空闲时长，不限制创建速率。模型、账户和宿主运行时的其他资源限制沿用 Pi 现有机制。
_避免使用_：模型预算、创建速率配额、空闲时长配额

**工作区**：
父会话及其子代理解析项目配置与资源时共同遵守的信任边界；工作区不拥有子代理，不会在不同父会话之间共享子代理，也不限制工具可访问的文件路径。
_避免使用_：子代理注册表、共享代理空间

**项目资源信任**：
根会话对固定工作区中的项目设置、扩展、技能、提示模板和系统提示是否可加载的统一决定；整棵代理树原样继承，代理模板不能提升或关闭。它是项目模板唯一的人工确认边界，信任后使用项目模板不再逐次确认。
_避免使用_：文件访问权限、模板信任覆盖

**上下文文件策略**：
代理模板决定当前子代理是否加载固定工作目录祖先链上的 `AGENTS.md` / `CLAUDE.md`；取值为 `enabled` 或 `disabled`，默认启用，各节点独立选择而不沿父子关系衰减。
_避免使用_：项目资源信任、文件访问授权

**模型选择策略**：
代理模板省略模型时捕获直接父会话在创建瞬间已经解析出的精确模型，显式配置时按当前模型目录和根策略选择精确 provider/model；创建前只做静态校验，不以子进程能力握手证明最终模型，也不在 Pi 运行时变化后持续复核。
_避免使用_：模型猜测、隐式默认模型

**思考等级策略**：
代理模板省略思考等级时捕获直接父会话在创建瞬间的实际等级，显式配置时校验固定枚举和当前已知模型支持范围；创建后不以能力握手或持续复核把该等级冻结为运行时常量。
_避免使用_：思考等级猜测、自动降级

**模板角色提示**：
代理模板 frontmatter 之后可为空的 Markdown 正文；它按系统提示模式追加到或替换当前子代理的项目与角色提示层，但不复制父会话系统提示或对话历史，也不能移除运行时控制契约。
_避免使用_：父提示继承、完整运行时提示替换

**系统提示模式**：
代理模板决定模板角色提示采用 `append` 或 `replace` 的方式；默认追加，替换仅覆盖项目与角色提示层，运行时安全、所有权、通信、工具契约及已启用的上下文文件始终保留。
_避免使用_：无边界系统提示覆盖、父系统提示继承

**技能与扩展继承**：
所有子代理按固定工作目录、统一项目资源信任和 Pi 的正常资源发现加载技能与扩展；代理模板不声明或限制二者。扩展注册的业务工具只参与创建时模板工具预检，创建后可随 Pi 的运行时机制变化。
_避免使用_：模板技能白名单、模板扩展白名单

**提示模板继承**：
所有子代理按固定工作目录、统一项目资源信任和 Pi 的正常资源发现加载斜杠提示模板；代理模板不声明或限制提示模板，也不自动把提示模板展开为子代理任务。
_避免使用_：代理模板继承、自动首条任务

**根环境快照**：
根会话建立代理树时捕获并供所有后代启动使用的环境变量集合；代理模板和中间父会话不能修改它，控制器只能追加协议运行所需且不可覆盖的内部元数据。
_避免使用_：模板环境变量、逐层环境扩张

**有效授权**：
控制器在创建节点时用于模板准入和树控制的授权快照；它同时受根会话上限、直接父会话当时的有效授权和代理模板请求约束。它不表示对 Pi 运行时业务资源的永久冻结。
_避免使用_：模板自带权限、子代理自授权

**逐级能力衰减**：
控制器拥有的直接父子权限和子代理管理能力沿父子关系只能保持或收窄的规则；根会话深度上限与祖先关闭状态不可突破。模板业务工具仍按此规则做创建时准入，但 Pi 后续动态装配的业务资源不属于持续审计范围。
_避免使用_：权限升级、后代权限覆盖

**工具可见性**：
某个代理模型当前能够发现和调用哪些工具。八个子代理管理工具、直接父回复能力和模板业务工具分别遵守各自的可见性规则；它们都不等同于宿主进程权限或文件系统沙箱。
_避免使用_：操作系统权限、路径隔离

**子代理管理能力**：
某个代理是否能够发现并调用模板目录查询、创建、消息、等待、中断、终止、状态和树视图这组子代理管理工具；它同时受直接父会话授权、模板开关和 `maxDepth` 约束，任何一项关闭都会移除整组工具。
_避免使用_：子代理权限提升、单个管理工具例外、直接父回复能力

**直接父回复能力**：
每个子代理主动向创建自己的直接父会话发送工作中回复的能力；它独立于子代理管理能力和模板工具集，因此叶节点与关闭管理能力的节点仍然保留，而根会话不具有该能力。
_避免使用_：第九个管理工具、模板业务工具、任意目标消息

**工具调用行**：代理管理工具在交互界面中表示一次调用请求的用户可读记录，属于界面呈现中的调用信息。
_避免使用_：原始工具 JSON、调用日志

**工具结果行**：代理管理工具在交互界面中表示一次调用完成状态及其摘要的用户可读记录，属于界面呈现中的结果信息。
_避免使用_：原始工具结果 JSON、工具日志

**模板工具集**：
代理模板声明的创建时必需业务工具完整集合；它不包含八个管理工具或直接父回复能力。创建前只需确认工具名已由 Pi 注册，并把完整集合用作子进程的初始工具请求，不要求它是父会话当前活动工具的子集；创建后 Pi 动态改变活动工具不会触发重新校验、降级或终止。
_避免使用_：工具最大集合、工具尽力集合、父工具子集校验

**运行时重载**：
Pi 在不重建子代理节点的情况下重新加载扩展、技能、提示模板、上下文文件等资源，或动态调整业务工具的行为；成功重载保留既有节点，只刷新未来创建使用的模板快照和 Pi 动态业务资源。它不重新打开创建事务，也不改变直接父子所有权、深度上限和祖先关闭的子代理管理能力。旧实例只在尚无新 factory 接管时使用有界 watchdog；新 factory 进入门禁后持有交接，完整注册生命周期处理器后才认领控制器，并等待 Pi 的 reload start 完成绑定。交接窗口中已收到但尚未进入父会话的子代理回复继续保留且不发送 reply ACK，新 API 绑定后按原序重试。新扩展实例未通过宿主兼容门禁时，既有代理树按根关闭语义清理，不能继续由旧控制器托管。
_避免使用_：重新创建、能力握手、模板再次准入、固定超时终止已认领树、向失效父 API 确认回复

**显式工具声明**：
每个代理模板必须明确给出模板工具集；省略不是继承全部工具的缩写。唯一合法空集合写法是精确 YAML 字符串 `tools: ""`；非空字符串在逗号拆分、裁剪并丢弃空项后没有工具名时属于无效模板。
_避免使用_：默认工具继承、隐式工具扩权

**模板标识**：
代理模板 Markdown 原始文件名去掉末尾 `.md` 后得到的值；公开创建工具以 `template_id` 精确引用它。模板标识区分大小写，不做字符限制、裁剪、大小写转换或 Unicode 归一化；它与子代理的代理标识和显示名称相互独立，模板 frontmatter 不再声明 `name`。
_避免使用_：模板显示名称、frontmatter 模板名称

**模板覆盖**：
用户模板与已获项目资源信任的项目模板具有相同模板标识时，只选择项目模板作为整棵代理树的有效模板候选项；两份模板不合并字段或正文。项目候选项配置无效时仍遮蔽用户候选项，不回退到用户模板，且该标识不可创建并返回 `template_invalid`。
_避免使用_：模板字段合并、同时暴露同名模板、无声回退

**有效模板目录**：
根会话发现并校验候选模板后，提供给整棵代理树后续查询和创建解析使用的格式有效模板集合。目录有效不等于某个调用者当前一定可以创建；模型、thinking 和管理能力仍在 `spawn_agent` 时预检。配置无效的候选文件不进入该目录或父代理的模板发现信息，但根会话首次发现完成时会通过一次汇总警告报告其来源、文件名和安全的简短原因；根会话 `/reload` 时以本轮成功扫描结果原子替换该目录，即使某一来源目录不可扫描也不保留旧的过期模板，既有节点不回溯改变。
_避免使用_：全部候选文件、无效模板列表

**模板目录查询**：
拥有子代理管理能力的模型通过 `get_agent_templates` 读取根权威当前有效模板目录的安全投影。成功正文直接是数组；每项只包含区分大小写的 `template_id`、可选 `description` 和始终存在的模板业务 `tools` 数组，不包含八个管理工具、模板正文、来源、模型或其他运行配置。空数组表示当前没有格式有效模板，此时不得调用 `spawn_agent`；非空项仍不替代创建预检。
_避免使用_：模板能力预检、无效模板枚举、模板正文查询

**模板发现快照**：
根控制器在首次发现或根 `/reload` 后原子发布的一组有效模板目录、无效候选模板诊断和来源级诊断。整棵树仅以当前快照解析后续创建；新快照不回溯改变既有节点，来源扫描失败时也不保留旧快照中来自失败来源的模板。
_避免使用_：模板实时视图、子代理本地目录、旧模板回退

**模板诊断索引**：
控制器内部保留的无效候选模板摘要，用于根会话 UI 警告。字段校验失败、文件不可读、符号链接目标失效和 frontmatter 解析失败都属于无效候选项；模板解析按来源优先级选中的候选项无效时才返回 `template_invalid`，被合法高优先级候选项遮蔽的无效候选项只用于诊断。它不属于有效模板目录或模型可发现信息，只保存来源、文件名和安全原因，不保存模板正文、底层路径或异常堆栈。
_避免使用_：无效模板目录、模板枚举

**模板来源诊断**：
已获信任模板来源目录存在但无法枚举时记录的无标识诊断；它会进入根会话模板诊断通知，但不进入模板诊断索引，不影响另一来源的发现，且未知模板请求仍返回 `template_not_found`。不存在的来源目录是正常空来源，不产生该诊断。
_避免使用_：目录无效模板、来源错误码

**模板诊断通知**：
根会话首次发现模板及每次根 `/reload` 刷新后，通过 Pi `ctx.ui.notify` 向用户显示至多一次当前模板发现诊断的汇总警告。诊断可来自无效候选模板或无法扫描的来源目录；不存在的模板目录是正常空来源，不产生诊断。通知是瞬时 UI 信号，不创建消息或会话条目，不进入系统提示或模型上下文。TUI 直接展示，RPC 作为独立扩展 UI 通知发送；无 UI 的 print/JSON 模式中该调用不产生可见输出，也不另设回退通道。子代理重载不显示该提示。
_避免使用_：诊断消息、上下文注入、持久化警告

**代理模板**：
创建子代理时复用的角色配置，可来自用户级或项目级资源；模板本身没有身份、对话状态或生命周期。
_避免使用_：子代理、代理实例

**父子监督通道**：
每个子控制器与直接父控制器之间独立于 Pi 任务 RPC 的本地双向控制通道；它只承载握手、生命周期、子树快照、分类的直接回复及其确认、逐跳权威请求与响应、重同步和关闭通知，不把控制帧本身写入会话消息或模型上下文。通道使用 `stream_id`、单向 `seq`、操作标识和有界 ACK，断序通过当前快照重同步。
_避免使用_：模型消息通道、共享事件总线、entry_appended 控制面

**监督帧闭集**：
父子监督通道唯一允许的十类帧：`hello`、`hello_ack`、`event`、`snapshot_request`、`snapshot`、`reply`、`control_request`、`control_response`、`ack` 和 `close`；闭集之外的控制语义必须拒绝，不能借用任务 RPC 或自由 JSON 扩张。
_避免使用_：开放事件总线、自定义控制消息、任务 RPC 控制帧

**代理树权威**：
一棵代理树中唯一裁决身份、配额、直接父关系、终止屏障和资源确认的顺序域；后代只能沿已认证的直接父子关系请求裁决，不能建立独立或更宽的树权威。
_避免使用_：本地子树权威、分布式配额、后代自授权

**本地监督中继**：
受管 RPC 节点内部连接真实子控制器与直接父监督通道的透明本地字节边界；它只认证连接并转发有界监督帧，不生成代理身份、裁决树状态或冒充子控制器。
_避免使用_：桥接控制器、代理节点、第二树权威

**子树快照修订**：
某个子控制器对以自身为作用域根的完整子树所维护的单调修订号；父控制器只接受大于已保存值的快照，重复或迟到快照丢弃。它不等同于根控制器对整棵树分配的 `tree_revision`。
_避免使用_：全局事件序号、消息标识、时间戳排序

**树修订**：
根或当前父控制器在原子接受一个完整子树变化后分配的公开 `tree_revision`；它用于让工具、常驻 widget 和遮罩面板判断快照新旧，不把不同节点的本地序号强行合并为一个跨进程事件流。
_避免使用_：节点 revision、传输 seq、增量日志游标

**常驻代理状态 widget**：
Pi TUI 中持续显示当前会话直接子代理状态的只读 `Agents` 区域；根会话显示根的直接子代理，普通父会话显示自身直接子代理，不渲染后代树，不提供控制按钮。它消费控制器确认的安全快照，更新不进入模型上下文。
_避免使用_：常驻代理树、全树控制台、代理操作面板

**代理树遮罩面板**：
由 `/agent` 命令打开的只读 TUI 临时视图；根会话查看整棵代理树，普通父会话查看自身子树。面板默认展开作用域直接子代理、折叠后代，支持滚动、左右展开/折叠和 `Esc` 关闭，终止记录归入默认折叠的 `finished` 区域，不改变任何控制权限。
_避免使用_：全局控制面板、越级控制台、复数形式的树查看命令

**代理树可观测性作用域**：
某个 UI 或 `get_agent_tree` 调用能够读取的树范围；根作用域为整棵树，普通父会话作用域为自身及后代子树。作用域只裁剪只读视图，不授予跨直接父子边界的发送、中断、终止或创建权限。
_避免使用_：权限作用域、全局树权限

**创建生命周期时间**：
从 `spawn_agent` 完成 Pi RPC 与父子监督通道握手、节点进入 `idle` 并成功返回的线性化瞬间开始，以单调时钟计算的子代理总时长；进入 `failed` 或 `terminated` 后冻结，不表示当前 prompt 或工具运行时长。
_避免使用_：创建请求耗时、任务耗时、模型响应耗时

**安全活动摘要**：
控制器依据已确认的工具边界映射出的短类别和计数，例如 `editing 2 files`；它不包含原始 prompt、assistant 正文、工具参数/结果、路径、环境变量或异常堆栈，只用于常驻 widget 和代理树遮罩面板。
_避免使用_：工具日志、实时输出、任务正文摘要

**RPC 监督器**：
围绕单个受管 RPC 节点的扩展控制模块；它是该节点状态变更命令的唯一调用方，负责启动握手、单节点命令串行、事件归一化、父子监督通道、优雅关闭、清理期限和资源确认，但不替代 Pi RPC 协议或直接裁决整棵树所有权。
_避免使用_：自定义 Pi RPC、全局进程管理器、模型控制器

**受管 RPC 节点**：
把一个子代理的 Pi 公共 `RpcClient` 命令面、平台进程树归属和退出观察绑定在同一启动事务中的运行载体；RPC 监督器只能使用这一个组合事实，不能把来自不同进程的客户端和树句柄拼接为可用节点。bridge 绑定后，监督 hello/ACK 必须能在节点仍为 `starting` 时双向通过，因为 bridge 启动完成本身依赖 child `session_start`；普通业务 RPC 仍等待节点进入 `ready`。
_避免使用_：独立 RPC 客户端与进程句柄组合、未受管 RPC 进程

**进程树适配器**：
由受管 RPC 节点启动模块使用的平台接缝，负责在目标进程运行前建立树归属，并以不透明树句柄执行优雅关闭、整树强制回收、退出等待、资源观察和句柄释放；Windows 首选 Job Object，Unix 类系统首选 process group/session，控制器不能读取 PID 或自行递归回收。
_避免使用_：PID 工具、直接子进程 kill、平台逻辑泄漏到领域层

**命令串行通道**：
单个 RPC 监督器内协调 prompt、steering、abort、优雅关闭和强制清理的唯一状态变更顺序域；只读状态查询和不同节点可并行，终止屏障建立后普通命令取消或拒绝。
_避免使用_：全局命令队列、跨节点串行、模型消息队列

**资源确认**：
监督器在进程、父子监督端点、本节点和全部后代都被观察为已退出后发布的事实；发送 EOF、abort 或强制信号本身不构成资源确认，也不能单独把节点推进到 `terminated`。
_避免使用_：信号成功、进程退出事件、优雅请求完成

**子代理扩展包**：
通过 Pi 原生 package 机制安装和发现、只以一个显式扩展入口提供子代理控制能力的可发行单元；它不拥有或自动写入代理模板、子代理配置及 Pi 用户或项目设置，模板和配置继续遵循各自既定来源。
_避免使用_：模板包、独立安装器、Pi 核心补丁

**宿主兼容门禁**：
子代理扩展注册任何公开能力或业务生命周期处理器前，对 Node/Pi 版本、必需 Pi API 和进程树平台适配器执行的全有或全无检查；任一要求不满足时整个扩展保持未激活，最多注册一次仅用于脱敏 UI 诊断的 `session_start` 桥，宿主 Pi 会话继续运行。该桥不启动控制器、监督器或子进程，也不写入模型上下文。
_避免使用_：部分工具降级、安装器 peer 检查、宿主会话失败
