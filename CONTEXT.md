# 父子代理协作上下文

本上下文定义 pi 子代理插件中父代理、子代理、会话、消息与运行事实的领域语言，避免把运行事实和消息递交混为一谈。

## 领域语言

**子代理**：代理树中的一个可独立运行、可被父代理管理的运行节点。

**子代理生命周期状态**：描述子代理自身是否正在启动、可用、执行工作、发生运行故障或已经终止的状态。它不描述某条消息的发送结果。
_Avoid_: 消息状态

**会话**：父代理与子代理之间持续存在的交流关系；会话中的消息不自动改变会话或生命周期状态。

**Pi 回合收束**：Pi 发出的真实 `agent_settled` 事实，表示当前回合已静止；只有 Pi 状态探测同时确认没有流式执行、原生压缩和待处理输入，且控制收尾允许时，才可据此登记 `idle`。它不自动生成报告或结果。

**Pi 原生压缩观察**：Pi 的 `compaction_start`、`compaction_end` 和 `get_state.isCompacting` 共同提供压缩运行事实。插件用这些事实校准生命周期和活动提示，不建立父子压缩事务，也不把本地观察状态作为消息发送的预拒绝依据。Pi 0.84.4 RPC 没有 `abort_compaction`，因此中断操作在观察到压缩活动时直接返回可重试的 `compaction_active`，不会把普通 `abort` 误当成压缩取消。

**消息发送**：父代理与子代理之间传递一次提示、控制内容或回复内容的操作。其成功含义由接收接口决定：Pi RPC 命令会返回明确接纳结果；Pi 扩展消息 API 只确认同步提交，不代表异步处理完成。

**Pi 命令接纳**：父端通过 Pi RPC 向子会话发送消息时，Pi 的命令响应明确表示该次调用已接纳或稳定拒绝；非压缩期间的中断同样由普通 `abort` 响应裁决。它们不表示模型已读、已开始处理或已完成后续工作。消息的原生 `compaction_active` 来自 Pi 的明确拒绝；压缩期间的中断结果来自插件对 Pi 原生压缩状态的当前观察。

**扩展消息提交**：子端回复通过父端 Pi `ExtensionAPI.sendMessage()` 注入。Pi 0.84.4 的该 API 返回 `void` 且异步错误另行报告，因此一次调用成功只表示父扩展运行时已同步接受提交；它不声称 Pi 已完成消息注入，也不可能同步传播 `compaction_active`。

**同步发送结果**：父到子的 Pi RPC 消息直接返回命令接纳或稳定拒绝；中断在原生压缩活动期间返回当前状态裁决，其他时候返回普通 `abort` 的命令结果。子到父的监督调用只返回扩展消息提交是否成功。调用方不等待模型处理、上下文观察或监督层 ACK。结果未知只结算为本次发送失败，不自动重试或重放正文。

**发送裁决点**：接收侧直接调用对应 Pi 接口。Pi RPC 消息以命令响应裁决；父端扩展消息以 fire-and-forget API 是否被同步调用裁决；压缩期间的中断以当前 Pi 原生压缩观察裁决。已经接纳或提交的数据不被后续控制回滚；`compaction_active` 只结算当前调用，不建立持久屏障。该裁决不约束多个数据事件的相互顺序，也不是持久消息队列或会话协调器。

**会话事件提交**：每次子端回复都是独立的父扩展消息提交。本 effort 不承诺父子消息之间、同向消息之间或并发事件之间的公开顺序，也不以一个事件删除、覆盖或重排另一个事件。`wait_agent` 只观察各次已提交事件与独立生命周期事实，具体到达顺序不是协议契约。

**监督传输元数据**：底层实现如需保留 `stream_id` 或帧序号，只用于传输帧完整性、连接管理或协议诊断；它们不进入消息信封，不承担会话 FIFO、重复/迟到裁决、ACK、发送窗口或应用消息重放职责。

**兼容性边界**：本 effort 的新监督协议只接受新规格定义的 wire、消息信封、生命周期投影和运行时交接契约；旧版本、旧运行时和旧 lease 不属于可接入的会话。版本不匹配是监督运行协议故障，不降级为消息发送失败。

**同规格 reload 交接**：reload lease 只转移同一兼容性边界内的运行时对象；协议版本、消息契约、生命周期投影或 transfer 契约不一致时不认领、不转换、不迁移旧会话。

**控制屏障**：只有实际接纳的中断或终止意图才会对后续消息、报告和续跑建立拒绝边界；`compaction_active` 阻塞结果不建立屏障，也不触发生命周期转换。终止屏障不可逆，并可升级已有的中断屏障；控制屏障不表示消息发送结果，也不改变已经接纳或提交的消息事件。

**传输确认（ACK）**：监督传输对帧或有序回复的协议层确认；它只说明对应传输事实已被处理，不等同于 Pi 接纳或生命周期状态。本 effort 的消息发送不使用该确认。

**最终报告（final_report）**：子代理在当前活动 Pi 回合中，认为已有内容值得交付时，通过显式工具发送给直接父代理的一条报告事件；它不自动改变子代理生命周期，也不结束当前 Pi 回合或会话。一次调用成功只表示父端扩展运行时接受了该消息提交；同一回合可发送多次，每次都是独立事件。

**发送重试**：同一次消息发送操作在内部进行的有限次再次尝试；重试过程不改变子代理生命周期状态。
