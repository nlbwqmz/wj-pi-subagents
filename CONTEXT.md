# 父子代理协作上下文

本上下文定义 pi 子代理插件中父代理、子代理、任务与消息协作的领域语言，避免把运行事实、任务结果和消息递交混为一谈。

## 领域语言

**子代理**：代理树中的一个可独立运行、可被父代理管理的运行节点。

**子代理生命周期状态**：描述子代理自身是否正在启动、可用、执行工作、发生运行故障或已经终止的状态。它不描述某条消息的发送结果，也不描述某个任务的结果。
_Avoid_: 任务状态、消息状态

**任务**：父代理交给子代理执行的一次工作委托；任务拥有独立于子代理生命周期的执行结果。

**任务结果**：一次任务最终成功、失败或被中断的结论，以及任务产生的最终内容（如有）。任务失败不等同于子代理运行故障。

**会话**：父代理与子代理之间持续存在的交流关系；会话中的消息不自动等同于一次任务的开始或结束。

**消息发送**：父代理与子代理之间传递一次提示、控制内容或回复内容的操作；它的成功只说明接收侧接受了该次发送，不代表后续任务处理已经完成。

**最终回复（final）**：子代理针对当前任务提交的最终任务结果，供父代理接收和继续处理。

**最终报告（final_report）**：子代理在认为当前工作已经可以交付时，通过显式工具发送给父代理的一条报告消息；它不自动改变子代理生命周期，也不强制结束当前 Pi 回合。

**发送重试**：同一次消息发送操作在内部进行的有限次再次尝试；重试过程不改变子代理生命周期状态。
