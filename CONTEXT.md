# 父子代理协作上下文

本上下文定义 pi 子代理插件中父代理、子代理、会话、消息与运行事实的领域语言，避免把运行事实和消息递交混为一谈。

## 领域语言

**子代理**：代理树中的一个可独立运行、可被父代理管理的运行节点。

**子代理生命周期状态**：描述子代理自身是否正在启动、可用、执行工作、发生运行故障或已经终止的状态。它不描述某条消息的发送结果。
_Avoid_: 消息状态

**会话**：父代理与子代理之间持续存在的交流关系；会话中的消息不自动改变会话或生命周期状态。

**Pi 回合收束**：Pi 发出的真实 `agent_settled` 事实，表示当前回合已静止；只有待处理输入、压缩屏障和控制收尾均允许时，才可据此登记 `idle`，它不自动生成报告或结果。

**上下文一致性屏障**：父子双方在压缩期间共同建立的暂时协调边界；它只保护上下文顺序和压缩收尾，不是消息结果、任务状态或生命周期状态。

**消息发送**：父代理与子代理之间传递一次提示、控制内容或回复内容的操作；它的成功只说明接收侧 Pi 已接受该次发送，不代表后续处理已经完成。

**Pi 接纳**：接收侧 Pi 的消息接口正常返回，表示该次消息已被接收侧会话接受；它不表示模型已读、已开始处理或已完成后续工作。

**同步接纳响应**：每次消息发送在调用链内直接得到接收侧 Pi 已接纳或稳定拒绝的结果；调用方不等待模型处理、上下文观察或监督层 ACK。响应未知只结算为本次发送失败，不自动重试或重放正文。

**接纳裁决点**：同一接收侧只在数据操作与控制屏障竞争时建立瞬时的接纳边界；已被 Pi 接纳的数据不被控制回滚，屏障建立后新发起的数据被拒绝。它不约束多个已接纳数据事件的相互顺序，也不是持久消息队列或会话协调器。

**会话事件接纳**：每次消息发送都是独立的同步 Pi 接纳操作；本 effort 不承诺父子消息之间、同向消息之间或并发事件之间的公开顺序，也不以一个事件删除、覆盖或重排另一个事件。`wait_agent` 只观察各次已接纳事件与独立生命周期事实，具体到达顺序不是协议契约。

**监督传输元数据**：底层实现如需保留 `stream_id` 或帧序号，只用于传输帧完整性、连接管理或协议诊断；它们不进入消息信封，不承担会话 FIFO、重复/迟到裁决、ACK、发送窗口或应用消息重放职责。

**兼容性边界**：本 effort 的新监督协议只接受新规格定义的 wire、消息信封、生命周期投影和运行时交接契约；旧版本、旧运行时和旧 lease 不属于可接入的会话。版本不匹配是监督运行协议故障，不降级为消息发送失败。

**同规格 reload 交接**：reload lease 只转移同一兼容性边界内的运行时对象；协议版本、消息契约、生命周期投影或 transfer 契约不一致时不认领、不转换、不迁移旧会话。

**控制屏障**：已被接纳的中断或终止意图，对后续消息、报告和续跑建立拒绝边界；终止屏障不可逆，并可升级已有的中断屏障。它不表示消息发送结果，也不改变已经接纳的消息事件。

**传输确认（ACK）**：监督传输对帧或有序回复的协议层确认；它只说明对应传输事实已被处理，不等同于 Pi 接纳或生命周期状态。本 effort 的消息发送不使用该确认。

**最终报告（final_report）**：子代理在当前活动 Pi 回合中，认为已有内容值得交付时，通过显式工具发送给直接父代理的一条报告事件；它不自动改变子代理生命周期，也不结束当前 Pi 回合或会话。一次调用成功只表示接收侧 Pi 接纳了该消息；同一回合可发送多次，每次都是独立事件。

**发送重试**：同一次消息发送操作在内部进行的有限次再次尝试；重试过程不改变子代理生命周期状态。
