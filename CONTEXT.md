# Pi 子代理协作

本上下文描述 Pi 父会话与其临时子代理之间的协作关系，统一规格和后续实现所使用的领域语言。

## 领域语言

**父会话**：
对某个子代理而言，创建并直接拥有它的 Pi 会话；根 Pi 会话和未达到深度上限的子代理都可以成为父会话，且每个父会话只直接控制自己的直接子代理。
_避免使用_：共享会话、全局会话

**子代理**：
归属于唯一父会话、拥有独立上下文和身份的临时协作实体；它可在父会话内接受多次交互并创建下一层子代理，但不会跨根 Pi 会话结束或恢复而持久化，且只能与直接父会话和直接子代理通信。
_避免使用_：持久代理、共享代理、对等代理

**代理标识**：
在一棵代理树内唯一标识某个子代理的 `agent_id` 值；该值是 UUID，控制器创建新节点时使用随机 UUID v4，并以 RFC 9562 canonical 小写文本格式（`xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`）传输，不带 `agent_` 前缀。它在节点生命周期内保持稳定，节点终止后也不会在当前根会话期间复用。控制操作通过代理标识寻址，显示名称不承担身份含义，代理标识本身不表达父子关系、深度或创建顺序；格式错误的调用参数属于 `invalid_argument`，格式正确但未注册的值属于 `agent_not_found`。
_避免使用_：以代理名称寻址、以层级路径寻址

**控制调用结果**：
父会话发起的一次控制工具调用是否成功完成；它由调用结果的 `ok` 表达，与目标子代理当前是否健康相互独立。

**节点故障**：
子代理当前生命周期中的控制面故障，例如 RPC 断开或启动超时；它可以作为状态查询成功结果中的 `data.error` 出现，不等同于控制调用失败。

**代理树**：
由根 Pi 会话及其递归创建的子代理组成的临时层级；代理树只允许有直接父子关系的节点相互通信，终止任一子代理时会同时终止它拥有的整个子树。
_避免使用_：代理网络、对等代理组

**代理树视图**：
向根 Pi 会话展示整棵代理树拓扑和运行状态的只读信息；查看该视图不赋予根会话越级通信或直接控制后代的权限。
_避免使用_：全局控制台、跨层控制面板

**父子消息**：
父会话向直接子代理发送的任务通信；消息先由插件接纳到节点 mailbox，同一活动任务内可作为 steering，协作式中断栅栏后的消息属于后继任务。Pi 原生自动压缩期间接纳的消息只有在后续真实处理轮次开始或宿主真实静止后才可出队，压缩结束本身不证明消息已交付或模型已读取。
_避免使用_：对等消息、广播消息、Pi 已读取确认、压缩结束即已恢复

每条父子消息由控制器分配一个在根会话生命周期内唯一且不复用的消息标识；调用者不能自定义该标识，它只用于关联控制面事件，不表达消息内容或处理结果。

**逻辑任务**：
由稳定 `task_id` 标识的一段父子工作所有权；它可跨自动重试续轮、多条 steering 和 Pi 原生自动压缩保持不变，直到匹配的 final 提交或节点进入终态。协作式中断栅栏后的新消息使用后继任务标识，不得并入正在结束的任务。
_避免使用_：单次 prompt、处理轮次、消息标识

**子代理回复**：
子代理只向直接父会话发送的上行协作内容总称，分为工作中回复和最终答复；未经主动选择的思考过程、工具前说明、工具参数与工具结果不属于子代理回复，也不允许越级发往其他祖先。
_避免使用_：普通 assistant 自动转发、父代理调用、越级回传

**子代理回复展示**：
父会话界面对一条子代理回复的可读投影；发送者名称用于辅助识别，代理标识继续承担身份含义，展示内容不改变模型可见回复信封。
_避免使用_：名称寻址、回复协议改写

**工作中回复**：
子代理在当前处理继续进行时主动发出的必要纯文本协作消息；它只接受 `message`，不携带唤醒开关、图片或其他二进制载荷。仅当任务在最终答复前遇到必须由直接父代理处理或裁决的阻塞问题，或者直接父代理明确要求过程回报时才能发送；不得用于常规进度、心跳、阶段性总结、完成通知或替代最终答复。任何已接纳的工作中回复都会触发直接父代理处理并结束一次包含该节点的 `wait_agent`，但不提交逻辑任务，也不声明任务已经完成。
_避免使用_：最终结果、隐式过程输出、普通进度、回复即暂停

**多目标等待**：
直接父代理在一次 `wait_agent` 调用的 `agent_ids` 中观察一个或多个直接子代理；所有目标共享一个观察期限，任一目标的工作中回复、任务提交、挂起或终态按 first-event 语义原子结束等待。timeout 只返回完整目标集合，不伪造获胜节点，也不改变任何节点生命周期。
_避免使用_：逐个串行等待、全员屏障、timeout 即终止

**等待批次**：
Pi 最终持久化的一条 assistant message 及其中全部直接工具调用构成的调度批次；其 session entry ID 是批次身份，`toolCallId` 是批次内调用身份。同批次误生成的多个 `wait_agent` 由插件合并为一个多目标等待，未包含获胜节点的 sibling 以 `batch_released` 结束；该 outcome 只表达工具协调，不是子代理生命周期状态。
_避免使用_：时间窗口批次、供应商原始协议、全局任意回复唤醒

**处理轮次**：
一次实际 Pi agent loop，由随机 UUID v4 `turn_id` 标识；同一逻辑任务可以因自动重试、自动续轮或 Pi 原生自动压缩包含多个处理轮次，旧轮次 final 不能提交到新轮次。
_避免使用_：逻辑任务、会话标识、消息标识

**结构化回复信封**：
直接父会话模型可见的 `pi-subagent.reply` JSON 对象；工作中回复与最终答复都携带固定 schema/version、`kind`、`agent_id`、`task_id` 和 `turn_id`，final 另携带幂等 `commit_id`。业务正文只放在自然语言 `text` 中；未知扩展字段可忽略，但必需字段、枚举、标识、文本和字段关系必须通过统一 codec 校验，`images` 字段会被显式拒绝。
_避免使用_：显示文案协议、任意业务 JSON、传输帧

**最终答复**：
子代理逻辑任务在结束边界由运行时准备且至多提交一次的最终上行事实；它用 `run_state` 区分正常、失败和中断，用 `output_state` 区分是否保留可用文本。raw settlement 只形成 provisional candidate，只有匹配任务/轮次的 final 已被直接父会话接纳后才完成提交；模型不能自行声明这些状态。
_避免使用_：工作中汇报、重复 final、raw settlement 即完成、说明性占位正文

**最终提交**：
同一 `commit_id` 从 prepared 单调推进到 accepted 的任务完成事实；它同时要求可提交的 settlement 与直接父会话接纳，提交后才产生 `last_task` 并允许静止节点进入 `idle`。
_避免使用_：raw settlement、传输 ACK、父会话已看到正文

**收尾唤醒屏障**：
child runtime 在当前任务收尾时暂缓会触发新模型轮次的后代回复；raw settlement 只建立 candidate 并立即返回，final 在后台 outbox 等待直接父会话 ACK。新轮或 `overflow` 且 `willRetry` 的原生自动压缩会撤销旧 candidate；阈值压缩可以保留安全候选。真正 final 提交后才放行，无法形成或确认时进入失败态。
_避免使用_：settled handler 内等待 ACK、提前放行、失败后自动续跑

**终止通知**：
直接父运行时针对节点 runtime fault 生成的独立 `pi-subagent.terminal` JSON 事实；它固定表达节点 `failed` 和 `runtime_fault`，不属于 child reply 或其序号域，不携带原始错误或业务正文，主动终止也不生成该通知。
_避免使用_：child final、主动终止回执、原始异常消息

**协作式中断**：
父会话请求直接子代理停止当前处理但保留节点及其上下文；其完成依赖当前处理响应取消，不承诺清除已接受的父子消息、立即空闲或永久结束节点。
_避免使用_：终止、暂停、清空队列

**终止**：
永久结束一个子代理及其整个子树并回收相关运行资源；终止后的节点不能继续接收父子消息，也不能恢复为可工作状态。
_避免使用_：中断、暂停、隐藏

**终止屏障**：
终止控制器在一个线性化点建立的不可逆关闭意图；它固定目标及当时已登记的后代，拒绝屏障后的普通消息和创建，并阻止迟到事件把节点恢复为可工作状态。屏障不等同于资源已经回收。
_避免使用_：软关闭标记、暂停状态、单节点退出

**清理期限**：
终止流程为优雅关闭、强制回收和最终资源确认设置的内部有界时间窗口；它独立于 `wait_agent.timeout_ms`，不由子代理或普通等待调用扩大。期限结束仍无法确认资源时保留 `terminating`，不假定已经回收。
_避免使用_：等待超时、空闲超时、模型请求超时

**强制进程树回收**：
清理期限到达后使用操作系统进程组能力一次性回收节点及其后代的阶段；Windows 首选 Job Object，Unix 类系统首选 process group/session。信号发送成功不等同于资源回收确认，平台能力不足时不能伪造 `terminated`。
_避免使用_：直接子进程 kill、PID 猜测、单进程终止

**防孤儿清理**：
中间父代理异常退出后，直接父控制器为其已登记的全部后代建立终止屏障并执行级联回收的流程；故障父节点保留 `failed` 和名额，直到直接父会话显式终止并确认整棵子树资源回收。
_避免使用_：自动重启、孤儿代理、子树静默删除

**部分级联失败**：
同一终止子树中部分节点已确认回收、部分节点仍无法确认的状态；已回收节点先进入 `terminated` 并释放名额，未确认节点保持带 `termination_incomplete` 的 `terminating`，父节点要等全部后代完成。
_避免使用_：整批原子成功、失败即重启、忽略未回收节点

**生命周期状态**：
子代理对外可观察的八种阶段：`starting`、严格静止的 `idle`、有当前任务的 `working`、中断栅栏已生效的 `interrupting`、交付不可确认或维护失败的 `suspended`、控制面故障的 `failed`、资源回收中的 `terminating` 和已确认回收的 `terminated`。顶层状态与任务活动阶段正交；raw settlement、队列变化或 Pi 命令响应都不能单独伪造 `idle`。
_避免使用_：把队列当状态、把 raw settlement 当完成、把 suspended 当终态

**任务活动阶段**：
活动节点当前等待的安全事实，例如处理、工具执行、压缩、对账、finalizing、父端 ACK、维护失败或不确定交付；它不包含 prompt、业务正文、路径、工具参数或结果。
_避免使用_：生命周期状态、工具日志、任务正文摘要

**任务队列投影**：
节点原子公开的三类待处理计数：插件 mailbox、Pi 宿主队列和 reply outbox。`idle` 要求三者都为零；计数不证明模型已读取消息，也不携带正文或消息标识。
_避免使用_：单一 pending、任务完成状态、队列正文

**空闲保留**：
子代理进入 `idle` 后不会因空闲时间自动终止或回收；它持续保留到直接父会话显式终止，或根会话结束时统一清理。等待一次处理的超时只结束等待，不改变节点生命周期。
_避免使用_：空闲回收、等待超时即终止

**终止记录**：
子代理完成终止后，在当前根会话期间保留的身份与最终状态；它用于诊断和幂等控制，不代表仍有运行资源，不占直接子代理名额，也不能恢复或接收父子消息。
_避免使用_：活动代理、持久代理、可恢复节点

**深度上限**：
根 Pi 会话为整棵代理树设定且后代不能提高的最大子代理层级；根 Pi 会话的深度为零，每经过一层父子关系深度加一。根启动参数、已获信任项目配置、用户配置和内置默认值按顺序确定一次有效值，默认值为 `2`，代码硬上限为 `8`；被选中的配置层不可读、JSON 无法解析或值不在 `1..8` 时直接采用默认值而不回退低优先级层，显式根参数非法则拒绝启动。达到上限的子代理不再承担创建、通信或控制下一层子代理的职责。
_避免使用_：无限递归、固定层级

**配额配置诊断**：
根会话解析深度或数量配额配置时产生的用户界面警告。它报告不可读配置、JSON 解析失败、非法已知字段或未知字段被忽略，以及因故采用的默认值；它是 UI-only 信号，不创建会话消息或模型上下文。显式根启动参数非法时不产生运行中的诊断，而是拒绝启动。
_避免使用_：配置模型消息、静默配置回退

**直接子代理上限**：
每个父会话最多允许拥有的直接子代理数量；各父会话独立计数，不按整棵代理树中相同深度的节点总数汇总。根启动参数、已获信任项目配置、用户配置和内置默认值按顺序确定一次有效值，默认值为 `4`，代码硬上限为 `16`。
_避免使用_：层级总量、全树同层上限

**全树代理上限**：
一棵代理树中除根会话外、尚未进入 `terminated` 的全部节点共享的数量上限；它独立于每个父会话的直接子代理上限。根启动参数、已获信任项目配置、用户配置和内置默认值按顺序确定一次有效值，默认值为 `16`，代码硬上限为 `64`；创建预留全树名额，节点及其子树完成资源回收后释放。
_避免使用_：直接子代理上限、同时运行数

**配额耗尽行为**：
达到深度上限时立即拒绝且不可重试；直接子代理或全树名额耗尽时立即拒绝，待调用方显式终止节点并完成资源回收后再重试。配额耗尽不自动等待、排队或回收。
_避免使用_：配额自动调度、隐式资源回收

**并行处理**：
不同子代理可以同时处理任务；扩展不设独立的同时运行数上限。单个子代理的消息、中断和终止仍按其控制通道串行协调，向已在处理的节点发送 steering 不产生额外运行配额。
_避免使用_：全树并发配额、跨节点命令串行

**资源控制范围**：
本版本只限制代理树深度、每个父会话的直接子代理数和整棵树的未终止节点数；不按 token/费用限制模型成本，不限制空闲时长，不限制创建速率。模型、账户和宿主运行时的其他资源限制沿用 Pi 现有机制。
_避免使用_：模型预算、创建速率配额、空闲时长配额

**工作区**：
父会话及其子代理解析项目配置与资源时共同遵守的信任边界；工作区不拥有子代理，不会在不同父会话之间共享子代理，也不限制工具可访问的文件路径。
_避免使用_：子代理注册表、共享代理空间

**项目资源信任**：
根会话对固定工作区的项目级设置、包、扩展、技能、提示模板、系统提示和项目模板目录是否参与加载的统一结论；子节点从根环境快照获得同一结论，模板不能改写它。它只控制项目本地资源的装配，不是文件系统沙箱，也不决定用户级或全局资源、命令行显式 `-e` 来源是否可被加载；项目模板目录只在该结论为真时扫描。
_避免使用_：文件访问权限、模板信任覆盖、全局扩展信任

**扩展来源信任**：
模板 `extensions` 所引用的 npm、Git 或其他远程来源，与项目资源信任是两个独立边界。项目模板能够被发现，不等于它引用的远程代码获得认可；远程来源的选择、解析、下载和执行风险由提供该来源的一方单独承担。反过来，远程来源也不会使工作区获得项目资源信任；两者均不构成进程、网络或文件系统权限边界。
_避免使用_：项目模板自动认可远程代码、远程来源授予工作区信任

**上下文文件策略**：
模板 `contextFiles` 是布尔值，省略时为 `true`；设为 `false` 时子进程传入 `--no-context-files`，不加载固定工作目录祖先链上的 `AGENTS.md` / `CLAUDE.md`。它只影响该节点的上下文文件发现，不改变项目资源信任或文件访问范围，也不沿父子关系逐层计算。
_避免使用_：项目资源信任、文件访问授权、枚举式上下文模式

**模型选择策略**：
模板省略 `model` 时，在创建该节点时捕获直接父会话当时的精确 `provider/model` 作为子进程请求；显式值同样只是对子进程的精确请求。父端只裁决模板结构、身份、树关系与配额，不查询父端已知 provider 或模型目录来证明可用性。子进程完成资源和扩展绑定后发布实际模型能力清单，父端据此核验请求；不匹配以 `capability_mismatch` 结束创建。
_避免使用_：父端 provider 预检、模型猜测、隐式模型降级

**思考等级策略**：
模板省略 `thinking` 时，在创建该节点时捕获直接父会话当时的实际等级；显式值必须是模板模式允许的固定枚举。父端不依据当前模型注册表或其支持范围作可用性预检；子进程绑定实际模型后在能力清单中报告实际等级，再由父端核验。创建成功后，Pi 运行时的后续变化不会触发持续复核、降级或终止。
_避免使用_：父端 thinking 预检、思考等级猜测、自动降级

**模板角色提示**：
代理模板 frontmatter 之后可为空的 Markdown 正文；它按系统提示模式追加到或替换当前子代理的项目与角色提示层，但不复制父会话系统提示或对话历史，也不能移除运行时控制契约。
_避免使用_：父提示继承、完整运行时提示替换

**系统提示模式**：
代理模板决定模板角色提示采用 `append` 或 `replace` 的方式；默认追加，替换仅覆盖项目与角色提示层，运行时安全、所有权、通信、工具契约及已启用的上下文文件始终保留。
_避免使用_：无边界系统提示覆盖、父系统提示继承

**技能与扩展装配**：
技能继续按固定工作目录、项目资源信任和 Pi 的普通资源发现装配。模板可通过 `extensions` 控制扩展发现：字段缺省时不传 `--no-extensions`，Pi 的自动发现保持生效；显式 `[]` 或非空数组都会传入 `--no-extensions`，前者不再追加模板来源，后者把每个来源作为重复的 `-e` 参数。无论这三种状态中的哪一种，本包自身的控制扩展都以单独的 `-e` 载入；Pi 的内联扩展也不属于被 `--no-extensions` 抑制的自动发现。模板扩展可注册父端未知的业务工具或 provider，实际结果只由 child 绑定后的能力清单裁决。
_避免使用_：父端扩展白名单预检、模板来源隐式合并、自动发现等同显式来源

**提示模板继承**：
所有子代理按固定工作目录、统一项目资源信任和 Pi 的正常资源发现加载斜杠提示模板；代理模板不声明或限制提示模板，也不自动把提示模板展开为子代理任务。
_避免使用_：代理模板继承、自动首条任务

**根环境快照**：
根会话建立代理树时捕获并供所有后代启动使用的环境变量集合；代理模板和中间父会话不能修改它，控制器只能追加协议运行所需且不可覆盖的内部元数据。
_避免使用_：模板环境变量、逐层环境扩张

**有效授权**：
控制器在创建节点时用于模板解析、身份、直接父关系、深度、配额和管理能力裁决的控制面授权快照；它同时受根会话上限、直接父会话当时的授权与模板布尔配置约束。它不授予业务工具、provider、远程来源或宿主权限，也不以父端的已知业务资源作为创建预检。
_避免使用_：模板自带业务权限、子代理自授权、父端资源证明

**逐级管理能力衰减**：
直接父子控制权限和子代理管理能力沿父子关系只能保持或收窄；根会话深度上限、祖先的管理能力和 `allowSubagents: false` 都不可突破。业务工具、provider 与 thinking 不是这条控制面衰减规则的输入，而是在 child 实际装配后通过能力清单核验。
_避免使用_：权限升级、后代权限覆盖、父工具子集校验

**工具可见性**：
某个代理模型当前能够发现和调用哪些工具。八个子代理管理工具、直接父回复能力和模板业务工具分别遵守各自的可见性规则；模板对业务工具的要求不等于父端当前已知或活动工具，实际集合以 child 绑定后的能力清单为准。它们都不等同于宿主进程权限或文件系统沙箱。
_避免使用_：操作系统权限、路径隔离、父端工具预检

**子代理管理能力**：
某个代理是否能够发现并调用模板目录查询、创建、消息、等待、中断、终止、状态和树视图这组子代理管理工具；它同时受直接父会话授权、模板 `allowSubagents` 布尔值和 `maxDepth` 约束，任一条件不成立都会移除整组工具。该组工具独立于直接父回复能力。
_避免使用_：子代理权限提升、单个管理工具例外、直接父回复能力

**直接父回复能力**：
每个子代理主动向创建自己的直接父会话发送必要纯文本工作中回复的能力；调用只接受 `message`，每条成功接纳的消息都唤醒直接父代理，不接受目标、唤醒开关、图片或其他二进制载荷。它独立于子代理管理能力和模板工具集，因此叶节点与关闭管理能力的节点仍然保留，而根会话不具有该能力。
_避免使用_：第九个管理工具、模板业务工具、任意目标消息、常规进度通道

**工具调用行**：代理管理工具在交互界面中表示一次调用请求的用户可读记录，属于界面呈现中的调用信息。
_避免使用_：原始工具 JSON、调用日志

**工具结果行**：代理管理工具在交互界面中表示一次调用完成状态及其摘要的用户可读记录，属于界面呈现中的结果信息。
_避免使用_：原始工具结果 JSON、工具日志

**模板工具集**：
`tools` 声明的是业务工具，不包含八个管理工具或直接父回复能力。字段缺省时不传 `--tools`，child 使用 Pi 的正常初始活动工具集合；显式 `[]` 或非空数组才传入严格 `--tools` allowlist，并与必需的协议工具合并，因此空业务集合不移除回复或已获授权的管理工具。父端不以已知或当前活动工具作预检；child 完成绑定后，能力清单必须满足显式业务集合，否则创建返回 `capability_mismatch`。创建成功后 Pi 对活动工具的后续变化不触发持续复核、降级或终止。
_避免使用_：工具最大集合、工具尽力集合、父工具子集校验

**运行时重载**：
根会话 `/reload` 先完整扫描并构造新的不可变模板发现快照，再原子替换供后续 `spawn_agent` 使用的发现与运行时配置。它只影响之后创建的节点，不接管、重绑或改写已经创建节点的模板、工具、模型、提示、上下文或生命周期；来源扫描失败时也不会悄然恢复旧快照中来自失败来源的条目。
_避免使用_：节点接管、跨实例交接、既有节点更新、旧快照回退

**模板 frontmatter**：
模板文件是 UTF-8 Markdown，开头必须有 YAML 映射 frontmatter。模式只允许八个字段：`description`、`extensions`、`tools`、`allowSubagents`、`contextFiles`、`systemPromptMode`、`model` 和 `thinking`；未知字段、重复键和 YAML merge 键都使候选模板无效。`description` 必填，裁剪后不得为空，且不得超过 512 个 Unicode code points。`tools` 与 `extensions` 都是可选的 YAML 字符串数组：缺省、精确 `[]` 和非空数组是三个需保留的不同状态；数组项须为非空字符串，裁剪后不得重复，`tools` 还不得包含系统保留工具名。`allowSubagents` 与 `contextFiles` 都是布尔值，缺省为 `true`。`systemPromptMode` 只能是 `append` 或 `replace`，缺省为 `append`；`model` 是可选的 `provider/model`，`thinking` 是可选的允许等级。frontmatter 后的 Markdown 正文可为空，最大为 64 KiB。
_避免使用_：额外 frontmatter 字段、字符串工具清单、模式枚举布尔配置

**Pi 扩展来源**：
每个 `extensions` 数组项按 Pi 的重复 `-e` 来源语法在 child 中解析：本地来源包括裸名称、相对或绝对路径及 `file:` URL；npm 来源使用 `npm:<package>[@<version-or-range>]`；Git 来源使用带 `git:` 前缀的 Pi 支持仓库写法，或显式 `http(s)://`、`ssh://`、`git://` 仓库 URL，并可采用 Pi 支持的 ref。模板发现只校验数组结构，不探测、下载或预检来源。相对本地来源以模板所在目录为基准解析，而不是 child 工作目录；远程来源保持原字符串。`--no-extensions` 只抑制 Pi 的自动扩展发现，不能抑制显式 `-e` 的本包入口、模板来源或 Pi 的内联扩展。
_避免使用_：相对扩展按 child 工作目录解析、模板发现下载来源、项目资源信任替代来源信任

**模板发现来源**：
根会话固定扫描用户目录 `~/.pi/agent/agents`，并且只在项目资源信任为真时扫描工作区 `.pi/agents`；两处都只读取目录直属的 UTF-8 `*.md` 常规文件或符号链接，不递归扫描。缺失目录是正常空来源，无法枚举或读取的来源只形成脱敏诊断。用户与项目候选项的同名遮蔽、有效性选择和快照发布均在根会话完成，child 不自行扫描模板目录。
_避免使用_：递归模板搜索、child 本地模板发现、未获信任项目模板

**模板标识**：
代理模板 Markdown 原始文件名去掉末尾 `.md` 后得到的值；公开创建工具以 `template_id` 精确引用它。模板标识区分大小写，不做字符限制、裁剪、大小写转换或 Unicode 归一化；它与子代理的代理标识和显示名称相互独立，模板 frontmatter 不再声明 `name`。
_避免使用_：模板显示名称、frontmatter 模板名称

**模板覆盖**：
用户模板与已获项目资源信任的项目模板具有相同模板标识时，只选择项目模板作为整棵代理树的有效模板候选项；两份模板不合并字段或正文。项目候选项配置无效时仍遮蔽用户候选项，不回退到用户模板，且该标识不可创建并返回 `template_invalid`。
_避免使用_：模板字段合并、同时暴露同名模板、无声回退

**有效模板目录**：
根会话发现并校验候选模板后，提供给整棵代理树后续查询和创建解析使用的格式有效模板集合。目录有效不等于调用者一定能够创建：`spawn_agent` 仍先裁决模板修订、直接父关系、管理能力、深度和配额，但不会用父端已知工具或 provider 作能力预检。child 完成资源绑定、监督通道建立并发布能力清单后，才核验业务工具、协议工具来源、模型和 thinking。配置无效的候选文件不进入该目录或父代理的模板发现信息，但根会话首次发现完成时会通过一次汇总警告报告其逻辑来源、文件名和安全的简短原因；根会话 `/reload` 以本轮完整扫描结果原子替换该目录，即使某一来源目录不可扫描也不保留旧条目。
_避免使用_：全部候选文件、父端能力目录、无效模板列表

**模板目录查询**：
拥有子代理管理能力的模型通过 `get_agent_templates` 读取根权威当前有效模板目录的安全投影。成功正文直接是数组；每项只包含区分大小写的 `template_id`、必填 `description`，以及仅在 frontmatter 声明时出现的业务 `tools` 与 `extensions` 数组，不包含八个管理工具、模板正文、模板目录、用户/项目来源标记、模型或其他运行配置。空数组表示当前没有格式有效模板，此时不得调用 `spawn_agent`；非空项仍不替代控制面裁决和 child 绑定后的能力核验。
_避免使用_：模板能力预检、无效模板枚举、模板正文查询

**模板发现快照**：
根控制器在首次发现或根 `/reload` 后原子发布的一组有效模板目录、无效候选模板诊断和来源级诊断。快照不可变，整棵树只以当前快照解析之后的创建；新快照不会改变已经创建节点，来源扫描失败时也不保留旧快照中来自失败来源的模板。
_避免使用_：模板实时视图、子代理本地目录、旧模板回退

**模板诊断索引**：
控制器内部保留的无效候选模板摘要，用于根会话 UI 警告。字段校验失败、文件不可读、符号链接目标失效和 frontmatter 解析失败都属于无效候选项；模板解析按来源优先级选中的候选项无效时才返回 `template_invalid`，被合法高优先级候选项遮蔽的无效候选项只用于诊断。它不属于有效模板目录或模型可发现信息，只保存来源、文件名和安全原因，不保存模板正文、底层路径或异常堆栈。
_避免使用_：无效模板目录、模板枚举

**模板来源诊断**：
已获信任模板来源目录存在但无法枚举时记录的无标识诊断；它会进入根会话模板诊断通知，但不进入模板诊断索引，不影响另一来源的发现，且未知模板请求仍返回 `template_not_found`。不存在的来源目录是正常空来源，不产生该诊断。
_避免使用_：目录无效模板、来源错误码

**模板诊断通知**：
根会话首次发现模板及每次根 `/reload` 刷新后，通过 Pi `ctx.ui.notify` 向用户显示至多一次当前模板发现诊断的汇总警告。诊断可来自无效候选模板或无法扫描的来源目录；不存在的模板目录是正常空来源，不产生诊断。通知是瞬时 UI 信号，不创建消息或会话条目，不进入系统提示或模型上下文。TUI 直接展示，RPC 作为独立扩展 UI 通知发送；无 UI 的 print/JSON 模式中该调用不产生可见输出，也不另设回退通道。该通知只由根会话发布。
_避免使用_：诊断消息、上下文注入、持久化警告

**代理模板**：
创建子代理时复用的角色配置，可来自用户级或项目级资源；模板本身没有身份、对话状态或生命周期。
_避免使用_：子代理、代理实例

**父子监督通道**：
每个 child 控制器与直接 parent 控制器之间独立于 Pi 任务 RPC 的本地双向控制通道；当前监督主版本为 `pi-subagent/11`，内部受管 bridge 主版本为 `pi-subagent/managed-rpc/3`。它在一个累计 ACK 顺序域中承载握手、生命周期、子树快照、一次性能力清单、任务 assignment/start、第四版结构化 reply、逐跳权威请求、child 发起的直接边压缩请求、parent 业务响应与关闭通知；业务 reply 不能越过对应 `task_started`，协调请求也不能把 transport ACK 当作业务确认。
_避免使用_：模型消息通道、共享事件总线、Pi 任务 RPC 控制帧、旧协议活动树

**能力清单**：
child 只有在普通监督通道 ready 且 Pi 已完成资源、扩展、工具和模型绑定后，才通过一次 `capability` 帧发布受限的内部快照。快照包含监督版本、业务与系统活动工具、系统工具来源映射、实际 provider/model/thinking，以及本包扩展入口路径；parent 严格校验结构与边界后缓存副本。它不属于模型可见模板目录、代理树状态或 UI 诊断，也不携带模板正文、凭据、异常或堆栈。
_避免使用_：父端资源推测、公开能力枚举、运行时持续能力流

**创建能力核验**：
parent 在启动前只做身份、模板结构与修订、直接父关系、管理能力、深度、配额和控制面协议检查，不以自己的已知工具、已注册 provider 或当前活动集合作为业务能力预检。child 完成实际绑定并发布可用的能力清单后，parent 才比较模板的显式工具约束、必需协议工具来源、模型和 thinking；清单有效但不满足请求时使用 `capability_mismatch`，并结束该次创建。进程不能启动、过早退出、监督通道或 Pi RPC 握手失败、协议建立失败，或没有获得可用清单时使用 `spawn_failed`；启动超时仍使用 `spawn_timeout`。对外诊断只给稳定错误码、固定消息和重试性，绝不暴露路径、模板或来源文本、凭据、provider/tool 探测细节、异常或堆栈。
_避免使用_：父端工具预检、父端 provider 预检、能力诊断泄露

**协调压缩事务**：
由不透明事务标识关联的一次会话局部直接边压缩协调；prepare 在当前会话冻结直接子 reply/final 入口、当前 child 自身的上行 reply/final，并在存在直接 parent 时请求唯一直接上游边，complete 以 `succeeded`、`failed`、`cancelled` 或 `not_started` 释放相同的本地边界。每条直接边独立完成业务确认或以同一事务 `not_started` 补偿；事务不递归传播到后代，不建立全树成员集合，也不等待 sibling 或非直接节点。它只协调会话本地入口和直接父子消息边界，不拥有压缩实现、逻辑任务、处理轮次或 continuation 决策；普通传输确认不等于 prepare/complete 业务确认。
_避免使用_：压缩恢复租约、任务事务、EventBus 已送达即完成、全局角色发现、递归子树事务

**直接边协调屏障**：
child 通过认证监督通道向唯一直接 parent 请求建立的会话局部屏障；parent 同步为该 child 安装下行 mailbox 令牌和上行 reply/final 令牌，再等待线性化点前的投递、prompt 启动和 Pi 内部消息队列静止。直接边静止至少要求 mailbox 没有 in-flight 交付、宿主待处理计数为零、没有等待中的 prompt start，并且不存在交付不确定或维护失败；Pi `queue_update.pendingMessageCount` 及 `get_state` 的同一计数是内部队列事实，RPC 命令成功本身不是排空证明。屏障令牌按非空且不超过 256 字符的事务标识叠加，释放一个事务不能释放其他事务；屏障后的新 prompt/steer 可可靠留在 mailbox 延迟投递，控制帧、业务响应、transport ACK 和关闭通知不受消息闸门阻塞。事务不递归传播到后代，不固定整棵子树成员，也不阻止创建新 child；根、父、子、孙和 sibling 会话可以各自并行压缩。complete 未确认时先以同事务 `not_started` 补偿；补偿仍没有业务响应才废止直接上游通道，父端无法闭合目标 child 的直接边时终止该 child。
_避免使用_：递归协调屏障、广播冻结、祖先子树锁、动态全树成员、只释放已收到 ACK 的成员

**原生压缩边界**：
Pi 因阈值或上下文溢出执行的原生自动压缩边界；它不转移逻辑任务所有权。压缩后的真实 `agent_start` 证明后续消息可进入当前处理轮次，真实 `agent_settled` 证明宿主已静止；在两者出现前，控制器不猜测 continuation 所有权或投递模式。
_避免使用_：压缩恢复租约、第三方 continuation marker、定时恢复、状态读取竞态

**根会话人工压缩边界**：
用户发起的 `/compact` 只属于根 Pi 会话的宿主生命周期，不是 managed child 的任务、mailbox 或 supervisor 事件。managed child 只有在协调压缩事务已经建立时才接纳扩展触发的 `manual` 压缩；未经协调的 child `manual` 事实仍是运行时不变量违约，而不是任务中断或 replacement final。
_避免使用_：未经协调的 child 人工压缩、压缩中断任务、同轮 replacement final

**监督帧闭集**：
父子监督通道唯一允许的十七类帧：`hello`、`hello_ack`、`event`、`snapshot_request`、`snapshot`、`capability`、`reply`、`task_assignment`、`task_started`、`control_request`、`control_response`、`compaction_prepare`、`compaction_prepared`、`compaction_complete`、`compaction_completed`、`ack` 和 `close`；闭集之外的控制语义必须拒绝。
_避免使用_：开放事件总线、自定义控制消息、任务 RPC 控制帧

**代理树权威**：
一棵代理树中唯一裁决身份、配额、直接父关系、终止屏障和资源确认的顺序域；后代只能沿已认证的直接父子关系请求裁决，不能建立独立或更宽的树权威。
_避免使用_：本地子树权威、分布式配额、后代自授权

**本地监督中继**：
受管 RPC 节点内部连接真实子控制器与直接父监督通道的透明本地字节边界；它只认证连接并转发有界监督帧，不生成代理身份、裁决树状态或冒充子控制器。
_避免使用_：桥接控制器、代理节点、第二树权威

**子树快照修订**：
某个子控制器对以自身为作用域根的完整子树所维护的单调修订号；父控制器只接受大于已保存值的快照，重复或迟到快照丢弃。它不等同于根控制器对整棵树分配的 `tree_revision`。
_避免使用_：全局事件序号、消息标识、时间戳排序

**树修订**：
根或当前父控制器在原子接受一个完整子树变化后分配的公开 `tree_revision`；它用于让工具、常驻 widget 和遮罩面板判断快照新旧，不把不同节点的本地序号强行合并为一个跨进程事件流。
_避免使用_：节点 revision、传输 seq、增量日志游标

**常驻代理状态 widget**：
Pi TUI 中持续显示当前会话直接子代理状态的只读 `Agents` 区域；根会话显示根的直接子代理，普通父会话显示自身直接子代理，不渲染后代树，不提供控制按钮。它消费控制器确认的安全快照，更新不进入模型上下文。
_避免使用_：常驻代理树、全树控制台、代理操作面板

**代理树遮罩面板**：
由 `/agent` 命令打开的只读 TUI 临时视图；根会话查看整棵代理树，普通父会话查看自身子树。面板默认展开作用域直接子代理、折叠后代，支持滚动、左右展开/折叠和 `Esc` 关闭，终止记录归入默认折叠的 `finished` 区域，不改变任何控制权限。
_避免使用_：全局控制面板、越级控制台、复数形式的树查看命令

**代理树可观测性作用域**：
某个 UI 或 `get_agent_tree` 调用能够读取的树范围；根作用域为整棵树，普通父会话作用域为自身及后代子树。作用域只裁剪只读视图，不授予跨直接父子边界的发送、中断、终止或创建权限。
_避免使用_：权限作用域、全局树权限

**创建生命周期时间**：
从 `spawn_agent` 完成 Pi RPC 与父子监督通道握手、节点进入 `idle` 并成功返回的线性化瞬间开始，以单调时钟计算的子代理总时长；进入 `failed` 或 `terminated` 后冻结，不表示当前 prompt 或工具运行时长。
_避免使用_：创建请求耗时、任务耗时、模型响应耗时

**安全活动摘要**：
控制器公开的任务活动阶段、可选稳定 `task_id` 和粗粒度工具类别/计数；它不包含 prompt、assistant 正文、工具参数/结果、路径、环境变量或异常堆栈，只用于状态、widget 和代理树面板。
_避免使用_：工具日志、实时输出、任务正文摘要

**RPC 监督器**：
围绕单个受管 RPC 节点的扩展控制模块；它是该节点状态变更命令的唯一调用方，负责启动握手、单节点命令串行、事件归一化、父子监督通道、优雅关闭、清理期限和资源确认，但不替代 Pi RPC 协议或直接裁决整棵树所有权。
_避免使用_：自定义 Pi RPC、全局进程管理器、模型控制器

**受管 RPC 节点**：
把一个子代理的 Pi 公共 `RpcClient` 命令面、平台进程树归属和退出观察绑定在同一启动事务中的运行载体；RPC 监督器只能使用这一个组合事实，不能把来自不同进程的客户端和树句柄拼接为可用节点。bridge 绑定后，监督 hello/ACK 必须能在节点仍为 `starting` 时双向通过，因为 bridge 启动完成本身依赖 child `session_start`；普通业务 RPC 仍等待节点进入 `ready`。
_避免使用_：独立 RPC 客户端与进程句柄组合、未受管 RPC 进程

**进程树适配器**：
由受管 RPC 节点启动模块使用的平台接缝，负责在目标进程运行前建立树归属，并以不透明树句柄执行优雅关闭、整树强制回收、退出等待、资源观察和句柄释放；Windows 首选 Job Object，Unix 类系统首选 process group/session，控制器不能读取 PID 或自行递归回收。
_避免使用_：PID 工具、直接子进程 kill、平台逻辑泄漏到领域层

**命令串行通道**：
单个 RPC 监督器内协调 mailbox 接纳、任务 assignment、Pi prompt/steer、interrupt、生命周期、reply 与 ACK 的唯一顺序域；公开接纳先于宿主交付，终止屏障建立后普通命令取消或拒绝。
_避免使用_：全局命令队列、跨节点串行、Pi 已读取确认

**资源确认**：
监督器在进程、父子监督端点、本节点和全部后代都被观察为已退出后发布的事实；发送 EOF、abort 或强制信号本身不构成资源确认，也不能单独把节点推进到 `terminated`。
_避免使用_：信号成功、进程退出事件、优雅请求完成

**子代理扩展包**：
通过 Pi 原生 package 机制安装和发现、在包根 `pi.extensions` 中声明一个实际扩展入口以提供子代理控制能力的可发行单元；它不拥有或自动写入代理模板、子代理配置及 Pi 用户或项目设置，模板和配置继续遵循各自既定来源。
_避免使用_：模板包、独立安装器、Pi 核心补丁

**子进程扩展入口**：
child 启动时固定解析当前包根实际声明的 `extensions/pi-subagents-wj.ts`，并将它作为显式 `-e` 来源传入 Pi，以建立受管监督、直接父回复和管理工具。入口解析不按开发或生产环境选择另一套自加载分支；受管 RPC bridge 独占 Pi RPC 流，扩展入口不接管该流。能力清单中的入口路径只用于内部核验，不能进入公开诊断或模型上下文。
_避免使用_：开发生产双入口、扩展自加载分支、公开入口路径

**宿主兼容门禁**：
子代理扩展注册任何公开能力或业务生命周期处理器前，对 Node/Pi 版本、必需 Pi API 和进程树平台适配器执行的全有或全无检查；任一要求不满足时整个扩展保持未激活，最多注册一次仅用于脱敏 UI 诊断的 `session_start` 桥，宿主 Pi 会话继续运行。该桥不启动控制器、监督器或子进程，也不写入模型上下文。
_避免使用_：部分工具降级、安装器 peer 检查、宿主会话失败
