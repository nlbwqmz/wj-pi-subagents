# 领域上下文

## 术语

- **父代理**：负责发起协作、接收结果并继续组织工作的 Pi 会话。
- **子代理**：由父代理委派、在独立 Pi 会话中工作的协作者。
- **工作状态**：子代理正在处理输入或继续执行工作，可以接收新的指导。
- **空闲状态**：子代理当前没有处理中的工作，可以开始新的输入。
- **消息提交**：父代理把一段指导交给子代理，提交成功只表示对方的运行接口接受了这次调用，不表示内容已经被模型读取或处理完成。
- **`send_message`**：父代理向子代理提交工作指导的动作；它关注一次指导是否被 Pi 接受，不代表任务完成。
- **`final_message`**：子代理主动提交一条结果声明的动作；它不会自动结束子代理，子代理仍可继续工作并提交后续结果。
- **调用成功**：Pi 的对应运行接口返回成功接纳。
- **调用错误**：Pi 的对应运行接口明确抛出或返回拒绝；错误只影响当前调用，除非同时确认了子代理本身发生故障。
- **最佳努力消息**：允许调用成功后的实际处理仍是异步的，也允许错误重试带来重复或顺序不确定的结果。
