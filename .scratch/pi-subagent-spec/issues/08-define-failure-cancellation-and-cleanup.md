# 定义中断、失败与级联清理语义

Type: grilling
Status: open
Blocked by: 01, 03, 06

## Question

父会话中断当前处理、终止节点、子进程异常退出、启动失败、RPC 协议损坏、父代理退出和根会话退出时，直接子代理及其后代分别进入什么状态、向上报告什么结果并执行哪些清理？规则必须避免孤立进程，并明确超时、重复控制请求、部分级联失败及 Windows 进程树清理的行为。

## Answer

<!-- 解决时填写。 -->

## Comments

- 2026-08-04：用户确认完成资源回收的 `terminated` 节点以会话内终止记录保留到根会话结束，用于状态诊断和幂等终止；它不占直接子代理名额、不接收消息、不恢复，根会话结束时统一释放。
